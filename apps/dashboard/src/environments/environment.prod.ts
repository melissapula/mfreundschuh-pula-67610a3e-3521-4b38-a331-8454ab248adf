// TODO(tradeoff): filled in with the real deployed api URL when we deploy
// (see the "Deploy live" step) — placeholder until then.
export const environment = {
  production: true,
  apiUrl: 'https://api.example.com/api',
};
